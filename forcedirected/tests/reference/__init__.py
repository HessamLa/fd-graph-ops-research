"""Frozen oracles for the parity tests of `forcedirected`.

A file here is a COPY that exists to preserve old behaviour, exactly like
`evaluator/tests/reference/`. Do not "fix" one: a change here silently
moves the thing the kernel is measured against.
"""
