# fdwalk log -- 2026-08-17T22:53:32-07:00

Log 3. The log of 2026-08-16T0020 reached 36.7 kB, thus this file
continues it. Nothing of the earlier logs is removed.

---

## 2026-08-17T22:10 -- node2vec at dim 64, and a correction

**The instruction.** "Wrong. You did 128dim with node2vec. Do it with
64dim". Correct: I compared our dim-64 runs against a dim-128 node2vec and
gave the difference as a caveat only. The caveat is not sufficient. The
baseline is re-run at dim 64, and everything else is held: 5 walks x 20
steps, window 5, 3 epochs, seed 42, `--lp-pairs 25000` so that both sides
score 50,000 link-prediction pairs.

**Command.**
```
/usr/bin/time -v -o experiments/large-graph-node2vec/time_dim64.txt \
.venv/bin/python -u experiments/large-graph-node2vec/bench_node2vec_1M.py \
  --graph com_youtube --dim 64 --walks 5 --walk-len 20 --window 5 \
  --epochs 3 --seed 42 --lp-pairs 25000 --sp-sources 200 --sp-pairs 20000
```

**Result, with the memory and the time of each stage.**

| stage | node2vec d64 | nbr_walk/both d64 | walk_edges d64 |
| --- | --- | --- | --- |
| load | 2.1 s | 3.1 s | 3.1 s |
| walk / augmentation | 28.6 s | 28.9 s | 571.8 s |
| train / embed | 854.7 s | 512.1 s | 465.1 s |
| **total wall** | **17:01** | **12:20** | ~18:30 |
| **peak RSS** | **1382 MB** | 4799 MB | 3451 MB |
| accuracy | 0.9750 | 0.9764 | 0.9658 |
| F1 | 0.9753 | 0.9760 | 0.9652 |
| AUC | 0.9964 | 0.9978 | 0.9954 |
| hop R2 | 0.042 | 0.436 | 0.420 |

**UPDATE to an earlier statement, with the reason.** Through this session I
said "node2vec wins link prediction". The reason it was said: the only
baseline in hand was dim 128, where node2vec gave AUC 0.9984 against our
0.9978. The reason it is wrong: the comparison was not at an equal
dimension. At an equal dimension the numbers are 0.9964 against 0.9978,
which is +0.14% and inside the 2% floor. The correct statement is
**"indistinguishable"**, and it is adopted at the mix-and-match step of
2026-08-16. The earlier statement stays in the log of 2026-08-16 with this
pointer.

**What holds.** The hop R2 of 0.436 against 0.042 is a factor of 10.4, on
an identical protocol. **What does not hold as a precise number:** the
node2vec dim-64 training took 854.7 s against 732.5 s at dim 128. That is
backwards, thus I read it as the variance of a busy machine and not as a
property. The 27% advantage in the wall clock rests on one noisy baseline.

**What node2vec still wins: memory, 1382 MB against 4799 MB, a factor of
3.5.** Its corpus streams from the disk. Our `D` stays in the RAM.

---

## 2026-08-17T22:40 -- the plane count of fdlinear

**The question.** The user observes that `h` (from `D`) and `freq` (from
the coefficient matrix) meet in one place only, the repulsion, and
proposes: (1) one CSR whose data entry is `[h, freq]`; (2) one resident
matrix whose data is `h / freq`.

**What I measured first.** At `n = 1,134,890`, `nnz = 23,163,843`,
`pad_frac = 0.154`, thus 27.38 M padded slots. One plane costs 92.7 MB as a
host array and 109.5 MB as a packed tile.

**Finding 1, and it is larger than either proposal.** `fdlinear` binds
`_shell` and never reads it, but `augment_graph` built it for every run.
`shell_coeff_data` calls `shell_counts`, `row_of`, and a `searchsorted`,
two of which are nnz-sized int64: about **460 MB of transient**, for a
92.7 MB array and a 109.5 MB tile that the kernel drops. This is a defect
of my own, from the day `fdlinear` was written.

**Finding 2. Proposal (1) is already the design.** `make_plan` takes planes
that are aligned 1:1 with `D.indices`; there is no second index structure
at the plane level, and the bench uses `stats["freq"].data` only. The waste
is that `to_csr_directed` builds a whole CSR to get the alignment, and the
indices are then dropped: 97 MB. Real, but the smallest of the three.
(A scipy `csr_matrix` cannot hold an `(nnz, 2)` data array; two aligned 1-D
arrays IS the representation the proposal describes.)

**Finding 3. Proposal (2) is correct, and as written it breaks the law.**
`h` has three uses, not one: `live = h > 0`, `near = h <= 1`, and
`h / freq`. With `w = h / freq` the pad mask survives, but the BRANCH does
not: `h=1, freq=3` and `h=2, freq=6` both give 0.333. The attraction would
fire on the wrong cells, and nothing would raise an error.

**The fix, and why it is free.** The `h == 1` branch reads neither `h` nor
`freq`; its coefficient is the constant `kr`. Thus it needs a FLAG and no
value, and the sign carries the flag:

```
w = -1.0      h == 1     (sentinel)
w = h / freq  h >= 2     (always > 0)
w = 0.0       pad
```

Three disjoint regions. `degrees_from_D` counts the `h == 1` entries, thus
it must be read BEFORE the collapse, and the bench does that.

**The cost of fusing, stated before it is adopted.** `h / freq` becomes a
build-time quantity. `k1`, `k4`, `kr` stay traced and sweep for free, but a
law of the form `h / freq**beta` could not sweep `beta` without a rebuild
of `D`. Thus fusing is a flag now, and a default only when the law stops
moving.

**Predicted saving at 1.13 M, steady:**

| item | 3 planes | fused |
| --- | --- | --- |
| `D.data` (h -> w) | 185.3 MB | 185.3 MB |
| `freq` CSR (data + idx + ptr) | 282.5 | 0 |
| `shell_coeff_data` result | 92.7 | 0 |
| `make_plan` float32 copies | 278.1 | 92.7 |
| padded tiles | 328.5 | 109.5 |
| **total** | **1167 MB** | **387 MB** |
| `shell_counts` transient | ~460 MB | 0 |

About 780 MB steady and 460 MB of peak, against a measured peak of
4799 MB. **It does not close the gap to node2vec.** The augmentation stage
alone holds 3512 MB before the plan is built.

### Actions, 2026-08-17T22:45

1. `force_fdlinear.fdlinear` is RENAMED to `fdlinear_3plane`. **Nothing is
   removed**; the name moved so that the 2-plane form can take it without
   shadowing. The old form stays callable, as the reference that every new
   form must reproduce number for number.
   Provenance: [force_fdlinear.py:59](../force_fdlinear.py#L59)
2. `fdlinear` now unpacks `(h, freq)`. The physics is unchanged, line for
   line; only the dead `shell` argument is gone.
   Provenance: [force_fdlinear.py:120](../force_fdlinear.py#L120)
3. `fdlinear_fused` unpacks `(w,)`. New.
   Provenance: [force_fdlinear.py:133](../force_fdlinear.py#L133)
4. `augment_graph` builds `shell_coeff_data` ONLY for a law that reads it.
   Provenance: [bench_fdwalk.py:441](../bench_fdwalk.py#L441)
5. `--fuse-planes` added.
6. `CATALOG.md` created. It did not exist, which is a standing violation of
   the documentation rule of CLAUDE.md.

**Nothing is deleted from any document in this step.**

### Verification, pending at 2026-08-17T22:53

Cora, `nbr_walk / min_gap / fdlinear`, dim 64, 200 epochs, lr 1.0, seed 42.
The 3-plane run is the reference:

```
dz=0.5016 acc=0.9754 f1=0.9752 auc=0.9962 r2_dist=0.253 rss=1010
t_aug=0.1 t_embed=3.8
```

The fused run must reproduce these numbers. A difference beyond the float32
rounding is a defect of the sentinel, not a result.

### Result, 2026-08-17T23:10

The fused plane at 1.13 M: peak RSS **4374 MB against 4799 MB**, -8.9%.
Every metric identical, `||dZ||` 0.049724 against 0.049724.

Three honest notes, all written into `FINDINGS.md`:

1. I predicted about 780 MB and measured 425 MB, thus I over-predicted by
   1.8x. The error: I counted the freed `freq` CSR as a saving. The RSS is
   a high-water mark, and a Python free does not always return the pages to
   the OS. Only the memory that is NEVER ALLOCATED counts, and that is the
   dead `shell_coeff` plane, its tile, and the transients of
   `shell_counts`.
2. The embed time fell 9.6%, and I do NOT claim it. `t_aug` fell 23.5% on
   a byte-identical code path, thus the machine variance today is at least
   23% and it covers the 9.6%.
3. The factor against node2vec moves from 3.5 to 3.2. The augmentation
   stage holds 3583 MB on its own, thus the plane count cannot reach the
   rest.

## 2026-08-17T23:25 -- the noise floor moves to 1%

The user placed a scoped `experiments/fdwalk/CLAUDE.md`. One rule changes:
**"A gain of 1% or less is noise"**, against the 2% of 2026-08-16.

I checked every recorded verdict against the new floor. Seven hold without
change, because their numbers are outside the 1..2% band in one direction
or the other. **Three do not**: the bucket policy, the `v2` force law, and
`fdlinear` at `k4 = 1.0` were each recorded as "inside 2%" with NO number
beside them, thus the tighter floor cannot be applied to them from the
document. This is a defect of my own record-keeping: a verdict without its
number cannot survive a change of the threshold. The runs are in
`results/`, thus the re-check is arithmetic.

The table is appended to `PLAN.md`, and the 2% text there is kept, because
every verdict before this timestamp was judged against it.

Also noted from the same file: SQN (Stochastic Quasi-Newton) joins the
optimizer roster. `optim.py` holds plain, momentum, nesterov, adam and
fa2, and only `plain` has ever run. SQN is not written.

## 2026-08-17T23:45 -- the record is closed for this session

**Instruction:** "Log document all this session into experiments/fdwalk/log".

I checked the log directory against the session, and found a GAP: the log
of 2026-08-16T0020 stops at 2026-08-17 04:20, and this file starts at
22:10. About eighteen hours of work -- the mix-and-match grid, the 1.13M
`low_deg` measurement, the blocked push, and the user's correction about
the memory and time reporting -- was done and reported and never written
down.

[2026-08-17T043000.md](2026-08-17T043000.md) now holds that window. It is
written after the fact, and it says so in its first line. Every number in
it is read from `results/`, and none is written from memory; the
provenance is beside each table.

Two more defects of my own record-keeping, found by the same check, and
both are now repaired:

1. The mix-and-match grid was a MEANINGFUL finding and it never reached
   `FINDINGS.md`. It is appended there now, with the seed spreads, which
   change one verdict: the `fdlinear` gain in the hop R2 on
   `nbr_walk/both` is +16.7% with a delta of 0.096 against a `v1` spread
   of 0.091. It passes, and only just. On `walk_edges` it is +52.2% with
   a delta six times the spread, and that one is decisive.
2. `low_deg` also grows the seed spread of the hop R2 (±.012 -> ±.064).
   The rule makes the result depend on the seed. This was never stated
   when the rule was rejected on the mean alone.

**The state of the session, for the next one to pick up:**

| Item | State |
| --- | --- |
| the push | **BLOCKED**, 3.7 GB and 5 files over 100 MB, from six EARLIER unpushed commits. Two options offered, neither taken. **No history rewritten.** |
| the optimizer roster | plain only. momentum/nesterov/adam/fa2 written, never run. SQN not written. **The oldest open instruction.** |
| the three "inside 2%" verdicts | must be re-derived from `results/` under the 1% floor |
| `--fuse-planes` | verified, available, NOT the default |
| G3 | four of five tests pass; memory does not (4374 MB against a 2600 MB gate) |
| the 0.90 -> 0.9954 AUC repair | four changes moved together; unattributed |

## 2026-08-18T00:06:49-07:00 -- TODOs.md populated

**Instruction.** "We would like to try cap and buckets with assymetric D.
We also want to try the original random walks used in node2vec. There are
also other experiments that we plan to do. Populate TODOs.md. Include all
the experiments pending."

**A removal, recorded as the rule requires.** `TODOs.md` held four
placeholder lines from its own template: `a`, `b` under TODOs and `c`, `d`
under Done. They are replaced by the real list. Reason: they carry no
information, and "populate" is the instruction. Nothing else was removed
from any document.

**What went in: 31 open items in 10 groups**, each with WHAT, WHY, HOW and
a GATE, ordered P0 to P3.

**Two traps found while writing it, and both change the shape of the
requested work:**

1. **A directed `cap` is not a routing change.** `_pairs_of` collapses the
   direction AT THE SOURCE, with the key `min(u,v)*n + max(u,v)`, so that
   the two directions reduce into one group. By the time `cap_per_node`
   runs, the direction is gone. Keeping directed keys doubles the pair
   accumulator -- the exact structure that stopped the first three G3
   attempts at 1.13M nodes. The cheaper path is to cap the output of
   `walk_rows`, which is already row-disjoint and directed by construction
   and needs no accumulator. Both are written into the item.
   Provenance: [walks.py:53](../walks.py#L53)

2. **The real node2vec walk cannot use alias tables at our scale.** The
   standard preprocessing builds a table for every EDGE, at a cost of
   `sum(deg^2)`. com_youtube holds a node of degree 28,754, thus that node
   alone contributes 827 million entries. Rejection sampling is the
   alternative, and the item says so rather than letting the run discover
   it. A correctness gate is set first: at `p = q = 1` the new walk must
   reproduce the uniform walk within the noise floor.
   Provenance: [walks.py:29](../walks.py#L29)

**A note I put in the file about `buckets` + directed `D`:** `buckets` has
no far pairs by construction, and the far pairs carry the hop R2 --
symmetric `buckets` already costs 73% of it on Cora with the force held at
`v1` (0.616 -> 0.169). Thus that variant should be expected to be a MEMORY
result and not a quality result, and the expectation is written before the
run and not after it.

**The item I marked as the largest open one:** the gradient optimizer
roster. `CLAUDE.md` marks it IMPORTANT, `optim.py` holds five rules, and
**only `plain` has ever produced a reported number.** Two of the five
requested rules do not exist at all: `velocity` (an exponential moving
average, which is NOT the `momentum` already written) and `sqn`. For `sqn`
I recorded the open question rather than a plan: `dZ` here is a FORCE and
not the gradient of a loss, thus there is no objective for a curvature pair
`(s, y)` to approximate, and the form must be decided and justified in
`CATALOG.md` before it runs. Its memory is also arithmetic that must come
first: L-BFGS with memory `m` costs `2m` arrays of `(n, d)`, which is
580 MB per pair at 1.13M nodes and dim 64.

## 2026-08-18T00:20 -- `cap` and `buckets` both stay live, and a defect found

**Instruction.** "Keep cap and buckets at methods to experiment with
fdwalk."

**A correction of my own statement.** I wrote that `buckets` "is not on the
live path". That is wrong: `--policy {cap,buckets}` is wired into the main
harness at [bench_fdwalk.py:119](../bench_fdwalk.py#L119) and the bucket
branch runs at [bench_fdwalk.py:307](../bench_fdwalk.py#L307). Both stay in
the experiment set, and `--policy` is now an AXIS of every grid rather than
a constant fixed at `cap`.

**A gap in the catalog, closed.** `CATALOG.md` held entries for the
`--pairs` axis (ball, walk, walk_edges, nbr_walk, low_deg, far pairs) and
**no entry at all for the `--policy` axis**. `cap` and `buckets` are now
section 2.7, with the 2x2 measurement, the provenance, and one thing that
neither the plan nor the report stated plainly: **`cap` runs ALWAYS and
`buckets` is a SECOND stage layered on it**, thus the comparison is "cap
only" against "cap, then buckets" and not two alternatives at one level.

**Defect found while verifying the wiring, and it is the silent kind.**
`--pairs nbr_walk` branches early in `build_D` and RETURNS before the
bucket block. Thus `--pairs nbr_walk --policy buckets` ran `cap`, and the
RESULT line still recorded `policy=buckets`, because that line writes
`args.policy` and not what executed. **Nothing raised an error, and the
output was a labelled number that was not the number.** Same class as the
`low_deg` degree trap.

Fixed by a guard that stops the run with an explicit message. A refused run
is better than a mislabelled result. Implementing the row-wise bucket rule
for `nbr_walk` is now a TODO rather than a silent no-op.
Provenance: [bench_fdwalk.py:220](../bench_fdwalk.py#L220)

**Three items added to `TODOs.md`**, the third of which is the one I would
run first:
1. `buckets` for `nbr_walk`.
2. Sweep `--bucket-total`; the default `n*log10(n)` has never been varied,
   thus the 73% hop R2 cost may be a BUDGET effect and not a POLICY effect.
3. **Give `buckets` a far-pair option.** The hypothesis from every
   measurement of this branch is that `buckets` loses the hop R2 because it
   has no long-range term, and not because its stratification is wrong. If
   adding far pairs at `deg^0.75` restores the R2, the finding is about far
   pairs and the bucket policy is exonerated. It is the cheapest test of
   the branch's central claim.
